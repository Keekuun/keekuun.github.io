<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://keekuun.github.io</id>
    <title>月上秦少</title>
    <updated>2023-01-10T07:08:15.446Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://keekuun.github.io"/>
    <link rel="self" href="https://keekuun.github.io/atom.xml"/>
    <subtitle>每一个不曾起舞的日子，都是对生命的辜负！</subtitle>
    <logo>https://keekuun.github.io/images/avatar.png</logo>
    <icon>https://keekuun.github.io/favicon.ico</icon>
    <rights>All rights reserved 2023, 月上秦少</rights>
    <entry>
        <title type="html"><![CDATA[真香！vue-cli-service项目升级vite]]></title>
        <id>https://keekuun.github.io/post/zhen-xiang-vue-cli-service-xiang-mu-sheng-ji-vite/</id>
        <link href="https://keekuun.github.io/post/zhen-xiang-vue-cli-service-xiang-mu-sheng-ji-vite/">
        </link>
        <updated>2023-01-10T07:03:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id=""><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/095afad2-2fff-4dc5-88bb-9366d8abcf01.png" alt="image" loading="lazy"></h2>
<h2 id="1-项目概览">1、项目概览</h2>
<figure data-type="image" tabindex="1"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/5db8b61f-f73a-435b-9c87-81afc4ab9429.png" alt="image" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/9261e685-febb-4297-b87c-6ddea0be312b.png" alt="image" loading="lazy"></figure>
<figure data-type="image" tabindex="3"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/aaf4306e-485c-4eb9-bbe1-fcd6c36c0081.png" alt="image" loading="lazy"></figure>
<p>admin-web管理后台是我们业务的管理中心，开发、运营、产品等工作人员会频繁使用，是我们目前业务的核心配置中心，这个项目从建立之初一直更新迭代，迄今为止有<strong>655</strong>次commits,<strong>681</strong>次CI，可见更新次数还是很频繁的。随着日积月累，代码量越来越多，vue单页面已经有了<strong>100+</strong> pages，<strong>30+</strong> vuecomponents，因此启动速度越来越慢。</p>
<h2 id="2-vue-cli-service和vite启动速度对比">2、vue-cli-service和vite启动速度对比</h2>
<p>将vue-cli-service升级切换到vite之后启动速度得到了巨幅提升，下面是对比结果：</p>
<p>1.使用vue-cli-service serve启动：</p>
<ul>
<li>
<p><strong>平均耗时：15s</strong></p>
</li>
<li>
<p><strong>最大耗时：40s</strong></p>
</li>
<li>
<p><strong>最小耗时：10s</strong></p>
</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/d2fc5dc7-3249-4dc6-b2b5-1acc21c8bebf.png" alt="image" loading="lazy"></figure>
<p>2.使用vite启动：</p>
<ul>
<li>
<p><strong>平均耗时：2s</strong></p>
</li>
<li>
<p><strong>最大耗时：5s</strong></p>
</li>
<li>
<p><strong>最小耗时：1s</strong></p>
</li>
</ul>
<figure data-type="image" tabindex="5"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/b9c8fa34-72b7-4e80-b817-4a714c01afdf.png" alt="image" loading="lazy"></figure>
<p><strong>可见启动速度提升差不多10倍，平均每次启动节省约13s。</strong></p>
<p><strong>下面介绍项目改造过程：</strong></p>
<h2 id="3-项目概览">3、项目概览</h2>
<h3 id="1-目录结构">1、目录结构</h3>
<p>改造前的目录结构，使用tree -aL 3 -I &quot;node_modules&quot; &gt; tree.txt输出：</p>
<pre><code>.
├── .editorconfig
├── .env.development
├── .env.production
├── .env.staging
├── .eslintignore
├── .eslintrc.js
├── .git
├── .gitignore
├── .gitlab-ci.yml
├── .idea
├── .travis.yml
├── Dockerfile.prod
├── Dockerfile.test
├── LICENSE
├── README.md
├── babel.config.js
├── build
├── dist
├── docker-compose.yml
├── jest.config.js
├── jsconfig.json
├── mock
├── nginx.conf
├── package.json
├── plopfile.js
├── postcss.config.js
├── public
│     ├── favicon.ico
│     └── index.html
├── scripts
│     └── notify.sh
├── src
│     ├── App.vue
│     ├── api
│     │   ├── activity.js
│     │   ├── config.js
│...      ...
│     ├── assets
│     ├── components
│     │   └── VideoPlay
│     ├── directive
│     ├── clipboard
│     ├── el-drag-dialog
│     ├── permission
│     ├── filters
│     ├── icons
│     │     ├── index.js
│     │     ├── svgo.yml
│			│			└── svg # 多个svg图标
│     │	      		├── 404.svg
│...         			...
│     │						└── zip.svg
│     ├── layout
│     ├── main.js
│     ├── permission.js
│     ├── router
│     │     ├── activity.js
│     │     ├── config.js
│...         ...
│     ├── settings.js
│     ├── store
│     │     ├── getters.js
│     │     ├── index.js
│			│			└── modules # 多个store
│     │	      		├── app.js
│...         			...
│     │						└── user.js
│     ├── store
│     ├── styles
│     ├── utils
│     └── views
│           ├── activity
│...         ...
│           └── withdrawal
├── vue.config.js
└── yarn.lock
</code></pre>
<p>使用vite改造后的目录：</p>
<pre><code>.
├── .editorconfig
├── .env.development
├── .env.production
├── .env.staging
├── .eslintignore
├── .eslintrc.js
├── .git
├── .gitignore
├── .gitlab-ci.yml
├── .idea
├── .travis.yml
├── Dockerfile.prod
├── Dockerfile.test
├── LICENSE
├── README.md
├── babel.config.js
├── build
├── dist
├── docker-compose.yml
├── index.html  # 新增
├── jest.config.js
├── jsconfig.json
├── mock
├── nginx.conf
├── package.json
├── plopfile.js
├── postcss.config.js
├── public
│     ├── favicon.ico
│     └── index.html
├── scripts
│     └── notify.sh
├── src
│     ├── App.vue
│     ├── api
│     │   ├── activity.js
│     │   ├── config.js
│...      ...
│     ├── assets
│     ├── components
│     │   └── VideoPlay
│     ├── directive
│     ├── clipboard
│     ├── el-drag-dialog
│     ├── permission
│     ├── filters
│     ├── icons
│     │     ├── index.js
│     │     ├── svgo.yml
│			│			└── svg # 多个svg图标
│     │	      		├── 404.svg
│...         			...
│     │						└── zip.svg
│     ├── layout
│     ├── main.js
│     ├── permission.js
│     ├── router
│     │     ├── activity.js
│     │     ├── config.js
│...         ...
│     ├── settings.js
│     ├── store
│     │     ├── getters.js
│     │     ├── index.js
│			│			└── modules # 多个store
│     │	      		├── app.js
│...         			...
│     │						└── user.js
│     ├── styles
│     ├── utils
│     └── views
│           ├── activity
│...         ...
│           └── withdrawal
├── vite.config.js # 新增
├── vue.config.js
└── yarn.lock
</code></pre>
<p>新增了两个文件：vite.config.js和 index.html</p>
<h3 id="2-node-package包">2、node package包</h3>
<p>改造前：package.json</p>
<pre><code>{
  &quot;name&quot;: &quot;admin-web&quot;,
  &quot;version&quot;: &quot;1.0.0&quot;,
  &quot;description&quot;: &quot;管理后台系统&quot;,
  &quot;scripts&quot;: {
    &quot;dev&quot;: &quot;vue-cli-service serve&quot;,
    &quot;lint&quot;: &quot;eslint --ext .js,.vue src&quot;,
    &quot;build:prod&quot;: &quot;vue-cli-service build&quot;,
    &quot;build:stage&quot;: &quot;vue-cli-service build --mode staging&quot;,
    &quot;preview&quot;: &quot;node build/index.js --preview&quot;,
    &quot;new&quot;: &quot;plop&quot;,
    &quot;svgo&quot;: &quot;svgo -f src/icons/svg --config=src/icons/svgo.yml&quot;,
    &quot;test:unit&quot;: &quot;jest --clearCache &amp;&amp; vue-cli-service test:unit&quot;,
    &quot;test:ci&quot;: &quot;npm run lint &amp;&amp; npm run test:unit&quot;
  },
  &quot;dependencies&quot;: {
    &quot;axios&quot;: &quot;0.18.1&quot;,
    &quot;clipboard&quot;: &quot;2.0.4&quot;,
    &quot;codemirror&quot;: &quot;5.45.0&quot;,
    &quot;core-js&quot;: &quot;^3.6.5&quot;,
    &quot;driver.js&quot;: &quot;0.9.5&quot;,
    &quot;dropzone&quot;: &quot;5.5.1&quot;,
    &quot;echarts&quot;: &quot;4.2.1&quot;,
    &quot;element-ui&quot;: &quot;^2.15.7&quot;,
    &quot;file-saver&quot;: &quot;2.0.1&quot;,
    &quot;fuse.js&quot;: &quot;3.4.4&quot;,
    &quot;js-cookie&quot;: &quot;2.2.0&quot;,
    &quot;jszip&quot;: &quot;3.2.1&quot;,
    &quot;md5&quot;: &quot;2.2.1&quot;,
    &quot;normalize.css&quot;: &quot;7.0.0&quot;,
    &quot;nprogress&quot;: &quot;0.2.0&quot;,
    &quot;path-to-regexp&quot;: &quot;2.4.0&quot;,
    &quot;screenfull&quot;: &quot;4.2.0&quot;,
    &quot;script-loader&quot;: &quot;0.7.2&quot;,
    &quot;sortablejs&quot;: &quot;1.8.4&quot;,
    &quot;tui-editor&quot;: &quot;1.3.3&quot;,
    &quot;v-viewer&quot;: &quot;^1.6.4&quot;,
    &quot;video.js&quot;: &quot;^7.18.1&quot;, # 删除，使用cdn
    &quot;videojs-contrib-hls&quot;: &quot;^5.15.0&quot;,  # 删除，使用@videojs/http-streaming cdn代替
    &quot;vue&quot;: &quot;2.6.10&quot;,
    &quot;vue-count-to&quot;: &quot;1.0.13&quot;,
    &quot;vue-router&quot;: &quot;3.0.2&quot;,
    &quot;vue-splitpane&quot;: &quot;1.0.4&quot;,
    &quot;vuedraggable&quot;: &quot;2.20.0&quot;,
    &quot;vuex&quot;: &quot;3.1.0&quot;,
    &quot;xlsx&quot;: &quot;0.14.1&quot;
  },
  &quot;devDependencies&quot;: {
    &quot;@vue/cli-plugin-babel&quot;: &quot;4.4.4&quot;,
    &quot;@vue/cli-plugin-eslint&quot;: &quot;4.4.4&quot;,
    &quot;@vue/cli-plugin-unit-jest&quot;: &quot;4.4.4&quot;,
    &quot;@vue/cli-service&quot;: &quot;4.4.4&quot;,
    &quot;@vue/test-utils&quot;: &quot;1.0.0-beta.29&quot;,
    &quot;autoprefixer&quot;: &quot;9.5.1&quot;,
    &quot;babel-eslint&quot;: &quot;10.1.0&quot;,
    &quot;babel-jest&quot;: &quot;23.6.0&quot;,
    &quot;babel-plugin-dynamic-import-node&quot;: &quot;2.3.3&quot;,
    &quot;chalk&quot;: &quot;2.4.2&quot;,
    &quot;chokidar&quot;: &quot;2.1.5&quot;,
    &quot;connect&quot;: &quot;3.6.6&quot;,
    &quot;eslint&quot;: &quot;6.7.2&quot;,
    &quot;eslint-plugin-vue&quot;: &quot;6.2.2&quot;,
    &quot;html-webpack-plugin&quot;: &quot;3.2.0&quot;,
    &quot;husky&quot;: &quot;1.3.1&quot;,
    &quot;lint-staged&quot;: &quot;8.1.5&quot;,
    &quot;mockjs&quot;: &quot;1.0.1-beta3&quot;,
    &quot;plop&quot;: &quot;2.3.0&quot;,
    &quot;runjs&quot;: &quot;4.3.2&quot;,
    &quot;sass&quot;: &quot;1.33.0&quot;,
    &quot;sass-loader&quot;: &quot;8.0.2&quot;,
    &quot;script-ext-html-webpack-plugin&quot;: &quot;2.1.3&quot;,
    &quot;serve-static&quot;: &quot;1.13.2&quot;,
    &quot;svg-sprite-loader&quot;: &quot;4.1.3&quot;,
    &quot;svgo&quot;: &quot;1.2.0&quot;,
    &quot;vue-template-compiler&quot;: &quot;2.6.10&quot;
  },
  &quot;browserslist&quot;: [
    &quot;&gt; 1%&quot;,
    &quot;last 2 versions&quot;
  ],
  &quot;engines&quot;: {
    &quot;node&quot;: &quot;&gt;=8.9&quot;,
    &quot;npm&quot;: &quot;&gt;= 3.0.0&quot;
  },
  &quot;lint-staged&quot;: {
    &quot;src/**/*.{js,vue}&quot;: [
      &quot;eslint --fix&quot;,
      &quot;git add&quot;
    ]
  },
  &quot;husky&quot;: {
    &quot;hooks&quot;: {
      &quot;pre-commit&quot;: &quot;lint-staged&quot;
    }
  }
}
</code></pre>
<p>改造后：（<strong>针对vue2项目使用vite2.x版本</strong>）</p>
<pre><code>{
  &quot;name&quot;: &quot;admin-web&quot;,
  &quot;version&quot;: &quot;2.0.0&quot;,
  &quot;description&quot;: &quot;管理后台系统&quot;,
  &quot;scripts&quot;: {
    &quot;dev&quot;: &quot;vue-cli-service serve&quot;,
    &quot;lint&quot;: &quot;eslint --ext .js,.vue src&quot;,
    &quot;lint:fix&quot;: &quot;eslint --ext .js,.vue src --fix&quot;,
    &quot;build:prod&quot;: &quot;vue-cli-service build&quot;,
    &quot;build:stage&quot;: &quot;vue-cli-service build --mode staging&quot;,
    &quot;preview&quot;: &quot;node build/index.js --preview&quot;,
    &quot;new&quot;: &quot;plop&quot;,
    &quot;svgo&quot;: &quot;svgo -f src/icons/svg --config=src/icons/svgo.yml&quot;,
    &quot;test:unit&quot;: &quot;jest --clearCache &amp;&amp; vue-cli-service test:unit&quot;,
    &quot;test:ci&quot;: &quot;npm run lint &amp;&amp; npm run test:unit&quot;,
    &quot;dev-vite&quot;: &quot;vite&quot;, # 新增
    &quot;build-vite&quot;: &quot;vite build&quot;,  # 新增
    &quot;preview-vite&quot;: &quot;vite preview&quot;  # 新增
  },
  &quot;dependencies&quot;: {
    &quot;axios&quot;: &quot;0.18.1&quot;,
    &quot;clipboard&quot;: &quot;2.0.4&quot;,
    &quot;codemirror&quot;: &quot;5.45.0&quot;,
    &quot;core-js&quot;: &quot;^3.6.5&quot;,
    &quot;driver.js&quot;: &quot;0.9.5&quot;,
    &quot;dropzone&quot;: &quot;5.5.1&quot;,
    &quot;echarts&quot;: &quot;4.2.1&quot;,
    &quot;element-ui&quot;: &quot;^2.15.7&quot;,
    &quot;file-saver&quot;: &quot;2.0.1&quot;,
    &quot;fuse.js&quot;: &quot;3.4.4&quot;,
    &quot;js-cookie&quot;: &quot;2.2.0&quot;,
    &quot;jszip&quot;: &quot;3.2.1&quot;,
    &quot;md5&quot;: &quot;2.2.1&quot;,
    &quot;normalize.css&quot;: &quot;7.0.0&quot;,
    &quot;nprogress&quot;: &quot;0.2.0&quot;,
    &quot;path-browserify&quot;: &quot;^1.0.1&quot;,
    &quot;path-to-regexp&quot;: &quot;2.4.0&quot;,
    &quot;screenfull&quot;: &quot;4.2.0&quot;,
    &quot;script-loader&quot;: &quot;0.7.2&quot;,
    &quot;sortablejs&quot;: &quot;1.8.4&quot;,
    &quot;tui-editor&quot;: &quot;1.3.3&quot;,
    &quot;v-viewer&quot;: &quot;^1.6.4&quot;,
    &quot;vue&quot;: &quot;2.6.10&quot;,
    &quot;vue-count-to&quot;: &quot;1.0.13&quot;,
    &quot;vue-router&quot;: &quot;3.0.2&quot;,
    &quot;vue-splitpane&quot;: &quot;1.0.4&quot;,
    &quot;vuedraggable&quot;: &quot;2.20.0&quot;,
    &quot;vuex&quot;: &quot;3.1.0&quot;,
    &quot;xlsx&quot;: &quot;0.14.1&quot;
  },
  &quot;devDependencies&quot;: {
    &quot;@babel/plugin-syntax-jsx&quot;: &quot;^7.18.6&quot;, # 新增
    &quot;@originjs/vite-plugin-require-context&quot;: &quot;^1.0.9&quot;, # 新增
    &quot;@vue/babel-helper-vue-jsx-merge-props&quot;: &quot;^1.4.0&quot;, # 新增
    &quot;@vue/babel-preset-jsx&quot;: &quot;^1.4.0&quot;, # 新增
    &quot;@vue/cli-plugin-babel&quot;: &quot;4.4.4&quot;,
    &quot;@vue/cli-plugin-eslint&quot;: &quot;4.4.4&quot;,
    &quot;@vue/cli-plugin-unit-jest&quot;: &quot;4.4.4&quot;,
    &quot;@vue/cli-service&quot;: &quot;4.4.4&quot;,
    &quot;@vue/test-utils&quot;: &quot;1.0.0-beta.29&quot;,
    &quot;autoprefixer&quot;: &quot;9.5.1&quot;,
    &quot;babel-eslint&quot;: &quot;10.1.0&quot;,
    &quot;babel-jest&quot;: &quot;23.6.0&quot;,
    &quot;babel-plugin-dynamic-import-node&quot;: &quot;2.3.3&quot;,
    &quot;chalk&quot;: &quot;2.4.2&quot;,
    &quot;chokidar&quot;: &quot;2.1.5&quot;,
    &quot;connect&quot;: &quot;3.6.6&quot;,
    &quot;eslint&quot;: &quot;6.7.2&quot;,
    &quot;eslint-plugin-vue&quot;: &quot;6.2.2&quot;,
    &quot;html-webpack-plugin&quot;: &quot;3.2.0&quot;,
    &quot;husky&quot;: &quot;1.3.1&quot;,
    &quot;lint-staged&quot;: &quot;8.1.5&quot;,
    &quot;mockjs&quot;: &quot;1.0.1-beta3&quot;,
    &quot;plop&quot;: &quot;2.3.0&quot;,
    &quot;progress-bar-webpack-plugin&quot;: &quot;^2.1.0&quot;, # 新增（可忽略）,查看vue-cli-service构建速度
    &quot;runjs&quot;: &quot;4.3.2&quot;,
    &quot;sass&quot;: &quot;1.33.0&quot;,
    &quot;sass-loader&quot;: &quot;8.0.2&quot;,
    &quot;script-ext-html-webpack-plugin&quot;: &quot;2.1.3&quot;,
    &quot;serve-static&quot;: &quot;1.13.2&quot;,
    &quot;svg-sprite-loader&quot;: &quot;4.1.3&quot;,
    &quot;svgo&quot;: &quot;1.2.0&quot;,
    &quot;vite&quot;: &quot;^2.9.15&quot;, # 新增
    &quot;vite-plugin-commonjs&quot;: &quot;^0.6.1&quot;, # 新增
    &quot;vite-plugin-components&quot;: &quot;^0.13.2&quot;, # 新增
    &quot;vite-plugin-env-compatible&quot;: &quot;^1.1.1&quot;, # 新增
    &quot;vite-plugin-optimize-persist&quot;: &quot;^0.1.2&quot;, # 新增
    &quot;vite-plugin-package-config&quot;: &quot;^0.1.1&quot;, # 新增
    &quot;vite-plugin-svg-icons&quot;: &quot;^2.0.1&quot;, # 新增
    &quot;vite-plugin-vue2&quot;: &quot;^2.0.3&quot;, # 新增
    &quot;vue-template-compiler&quot;: &quot;2.6.10&quot;
  },
  &quot;browserslist&quot;: [
    &quot;&gt; 1%&quot;,
    &quot;last 2 versions&quot;
  ],
  &quot;bugs&quot;: {
    &quot;url&quot;: &quot;https://github.com/PanJiaChen/vue-element-admin/issues&quot;
  },
  &quot;engines&quot;: {
    &quot;node&quot;: &quot;&gt;=8.9&quot;,
    &quot;npm&quot;: &quot;&gt;= 3.0.0&quot;
  },
  &quot;lint-staged&quot;: {
    &quot;src/**/*.{js,vue}&quot;: [
      &quot;eslint --fix&quot;,
      &quot;git add&quot;
    ]
  },
  &quot;husky&quot;: {
    &quot;hooks&quot;: {
      &quot;pre-commit&quot;: &quot;lint-staged&quot;
    }
  }
}
</code></pre>
<h3 id="3-入口文件indexhtml">3、入口文件index.html</h3>
<ol>
<li>位置变换:</li>
</ol>
<p>由 root/public/inde.html到 root/index.html</p>
<ol start="2">
<li>内容变化:</li>
</ol>
<p>改造前：</p>
<pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;meta charset=&quot;utf-8&quot;&gt;
    &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge,chrome=1&quot;&gt;
    &lt;meta name=&quot;renderer&quot; content=&quot;webkit&quot;&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no&quot;&gt;
    &lt;link rel=&quot;icon&quot; href=&quot;&lt;%= BASE_URL %&gt;favicon.ico&quot;&gt;
    &lt;title&gt;&lt;%= webpackConfig.name %&gt;&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;div id=&quot;app&quot;&gt;&lt;/div&gt;
    &lt;!-- built files will be auto injected --&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>改造后：</p>
<pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;meta charset=&quot;utf-8&quot;&gt;
    &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge,chrome=1&quot;&gt;
    &lt;meta name=&quot;renderer&quot; content=&quot;webkit&quot;&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no&quot;&gt;
    &lt;link rel=&quot;icon&quot; href=&quot;/favicon.ico&quot; /&gt;
    &lt;link href=&quot;https://unpkg.com/video.js/dist/video-js.css&quot; rel=&quot;stylesheet&quot;&gt;
    &lt;title&gt;melon admin&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;div id=&quot;app&quot;&gt;&lt;/div&gt;
    &lt;!-- built files will be auto injected --&gt;
    &lt;script type=&quot;module&quot; src=&quot;/src/main.js&quot;&gt;&lt;/script&gt;
    &lt;script src=&quot;https://unpkg.com/video.js/dist/video.js&quot;&gt;&lt;/script&gt;
    &lt;script src=&quot;https://unpkg.com/@videojs/http-streaming/dist/videojs-http-streaming.js&quot;&gt;&lt;/script&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h2 id="4新增vite配置文件">4.新增vite配置文件</h2>
<p>vite.config.js</p>
<pre><code>import { defineConfig } from 'vite'
import commonjs from 'vite-plugin-commonjs'
import ViteRequireContext from '@originjs/vite-plugin-require-context'
import viteComponents, {
  VuetifyResolver
} from 'vite-plugin-components'
import envCompatible from 'vite-plugin-env-compatible'
import OptimizationPersist from 'vite-plugin-optimize-persist'
import PkgConfig from 'vite-plugin-package-config'
import { createSvgIconsPlugin } from 'vite-plugin-svg-icons'
import { createVuePlugin } from 'vite-plugin-vue2'

import path from 'path'

// const { createVuePlugin } = require('vite-plugin-vue2')
// const path = require('path')
const REPLACEMENT = `${path.resolve(__dirname, './src')}/`
export default defineConfig({
  server: {
    host: '0.0.0.0',
    https: false,
    port: 8088,
    proxy: {
      '/melon.admin.s': {
        // 路径中有 /api 的请求都会走这个代理 , 可以自己定义一个,下面移除即可
        target: 'https://xxx.xxx.com', // 目标代理接口地址,实际跨域要访问的接口,这个地址会替换掉 axios.defaults.baseURL
        // target: 'http://localhost:9000', // 目标代理接口地址,实际跨域要访问的接口,这个地址会替换掉 axios.defaults.baseURL
        secure: false,
        changeOrigin: true, // 开启代理，在本地创建一个虚拟服务端
        ws: true //,       // 是否启用  websockets;
      }
    }
  },
  resolve: {
    alias: [
      {
        find: '@/',
        replacement: REPLACEMENT
      },
      {
        find: 'src/',
        replacement: REPLACEMENT
      },
      {
        find: /^~@\//,
        replacement: REPLACEMENT
      }
    ],
    extensions: ['.vue', '.js', '.jsx', '.mjs', '.ts', '.tsx', '.json', '.css', '.scss']
  },
  plugins: [
    createVuePlugin({ jsx: true }),
    viteComponents({
      customComponentResolvers: [
        VuetifyResolver()
      ]
    }),
    createSvgIconsPlugin({
      iconDirs: [path.resolve(__dirname, './src/icons/svg')],
      symbolId: 'icon-[dir]-[name]'
    }),
    commonjs(/* options */),
    ViteRequireContext(/* options */),
    envCompatible(),
    PkgConfig(),
    OptimizationPersist()
  ]
}
)
</code></pre>
<blockquote>
<p>至于为什么要这样配置，当然是一步步踩坑，填坑至此，说多了都是泪啊</p>
</blockquote>
<h2 id="5-切换vite问题汇总">5、切换vite问题汇总</h2>
<h3 id="1页面显示找不到此-localhost-页面">1.页面显示：找不到此 localhost 页面</h3>
<p><strong>需要把index.html从public文件夹移动到root目录下</strong></p>
<h3 id="2uri-malformed报错标题显示-webpackconfigname">2.'URI malformed'报错,标题显示&lt;%= webpackConfig.name %&gt;</h3>
<pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
  &lt;meta charset=&quot;utf-8&quot;&gt;
  &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge,chrome=1&quot;&gt;
  &lt;meta name=&quot;renderer&quot; content=&quot;webkit&quot;&gt;
  &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no&quot;&gt;
  &lt;link rel=&quot;icon&quot; href=&quot;&lt;%= BASE_URL %&gt;favicon.ico&quot;&gt;
  &lt;link href=&quot;https://unpkg.com/video.js/dist/video-js.css&quot; rel=&quot;stylesheet&quot;&gt;
  &lt;title&gt;&lt;%= webpackConfig.name %&gt;&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;app&quot;&gt;&lt;/div&gt;
&lt;!-- built files will be auto injected --&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p><strong>需要切换路径，</strong>&lt;%= webpackConfig.name %&gt;和&lt;%= BASE_URL %&gt;webpack能识别，vite识别不了，修改如下：</p>
<pre><code>&lt;!--    标题直接配置在儿--&gt;&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;meta charset=&quot;utf-8&quot;&gt;
    &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge,chrome=1&quot;&gt;
    &lt;meta name=&quot;renderer&quot; content=&quot;webkit&quot;&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no&quot;&gt;
    &lt;!--    public下的文件使用路径即可--&gt;
    &lt;link rel=&quot;icon&quot; href=&quot;/favicon.ico&quot; /&gt;
		&lt;!--    标题直接配置在儿--&gt;
    &lt;title&gt;admin web&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;div id=&quot;app&quot;&gt;&lt;/div&gt;
    &lt;!-- built files will be auto injected --&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="3访问成功单页面空白控制台无任何信息">3.访问成功，单页面空白，控制台无任何信息</h3>
<pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;meta charset=&quot;utf-8&quot;&gt;
    &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge,chrome=1&quot;&gt;
    &lt;meta name=&quot;renderer&quot; content=&quot;webkit&quot;&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no&quot;&gt;
&lt;!--    public下的文件使用路径即可--&gt;
    &lt;link rel=&quot;icon&quot; href=&quot;/favicon.ico&quot; /&gt;
    &lt;link href=&quot;https://unpkg.com/video.js/dist/video-js.css&quot; rel=&quot;stylesheet&quot;&gt;
&lt;!--    标题直接配置在儿--&gt;
    &lt;title&gt;melon admin&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;div id=&quot;app&quot;&gt;&lt;/div&gt;
    &lt;!-- built files will be auto injected --&gt;
    &lt;!--    以module的模式加载main.js--&gt;
    &lt;script type=&quot;module&quot; src=&quot;/src/main.js&quot;&gt;&lt;/script&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>vite以esm的形式加载js文件，<strong>需要手动引入</strong>一下。</p>
<blockquote>
<p>如果继续空白，先把报错的代码注释掉，面向谷歌编程，一个个解决就完事了</p>
</blockquote>
<h3 id="4vite无法识别alias路径">4.vite无法识别alias路径</h3>
<pre><code>[plugin:vite:import-analysis] Failed to resolve import &quot;@/styles/index.scss&quot; from &quot;src\main.js&quot;. Does the file exist?
</code></pre>
<p>加载alias文件路径失败，<strong>需要配置alias</strong>,vite才能识别</p>
<pre><code>import { defineConfig } from 'vite'

import path from 'path'
const REPLACEMENT = `${path.resolve(__dirname, './src')}/`
export default defineConfig({
  resolve: {
    alias: [
      {
        find: '@/',
        replacement: REPLACEMENT
      }
    ]
  }
)
</code></pre>
<h3 id="5vite无法自动查询文件后缀名文件not-found">5.vite无法自动查询文件后缀名，文件Not Found</h3>
<pre><code>GET http://localhost:8089/src/layout net::ERR_ABORTED 404 (Not Found)

import Layout from '/src/layout
</code></pre>
<p>直接导入某个目录下的index.js、index.css、index.json时可以省略具体的文件名，让webpack自动查找，vite无法识别，<strong>需要配置****extensions</strong>:</p>
<pre><code>import { defineConfig } from 'vite'

import path from 'path'
const REPLACEMENT = `${path.resolve(__dirname, './src')}/`
export default defineConfig({
  resolve: {
    alias: [
      {
        find: '@/',
        replacement: REPLACEMENT
      }
    ],
    extensions: ['.vue', '.js', '.jsx', '.mjs', '.ts', '.tsx', '.json', '.css', '.scss']
  }
)
</code></pre>
<h3 id="6vite识别不了-jsx-uncaught-referenceerror-react-is-not-defined">6.vite识别不了 jsx， Uncaught ReferenceError: React is not defined</h3>
<p><strong>需要配置jsx插件：</strong></p>
<pre><code>import { defineConfig } from 'vite'
import { createVuePlugin } from 'vite-plugin-vue2'

export default defineConfig({
  plugins: [
    createVuePlugin({ jsx: true }),
  ]
}
</code></pre>
<h3 id="7jsx语法未注明pluginvite-plugin-commonjs-unexpected-token">7.jsx语法未注明：[plugin:vite-plugin-commonjs] Unexpected token</h3>
<pre><code>if (icon) {
19 |        if (icon.includes('el-icon')) {
20 |          vnodes.push(&lt;i class={[icon, 'sub-el-icon']} /&gt;)
   |                      ^
21 |        } else {
22 |          vnodes.push(&lt;svg-icon icon-class={icon}/&gt;)}
</code></pre>
<script>标签下的js中包含jsx的语法**需要注明：****<script lang="jsx">**

### 9.vite无法识别require: Uncaught ReferenceError: require is not defined

commonjs使用require来加载js，vite直接加载esm，vite无法识别require,解决方式有二：

1.  **把commonjs的require方式改为esm的import方式**
    
2.  **使用插件使vite支持require**:
    

    import { defineConfig } from 'vite'
    import commonjs from 'vite-plugin-commonjs'
    
    export default defineConfig({
      plugins: [
        createVuePlugin({ jsx: true }),
        commonjs(/* options */)
        ...	...
      ]
    }

**当然****module.exports****定义的js文件强烈建议修改为****export default****拥抱ESM吧**

### 8.动态加载require无法识别：Uncaught ReferenceError: require is not defined at index.js:8:22

1.store/index.js

    import Vue from 'vue'
    import Vuex from 'vuex'
    import getters from './getters'
    
    Vue.use(Vuex)
    
    // https://webpack.js.org/guides/dependency-management/#requirecontext
    const modulesFiles = require.context('./modules', true, /\.js$/)
    
    // you do not need `import app from './modules/app'`
    // it will auto require all vuex module from modules file
    const modules = modulesFiles.keys().reduce((modules, modulePath) => {
      // set './app.js' => 'app'
      const moduleName = modulePath.replace(/^\.\/(.*)\.\w+$/, '$1')
      const value = modulesFiles(modulePath)
      modules[moduleName] = value.default
      return modules
    }, {})
    
    const store = new Vuex.Store({
      modules,
      getters
    })
    
    export default store


这里使用了webpack的requirecontext来读取store/modules目录下的多个store，否则需要一行行手动引入。

2.icons/index.js

    import Vue from 'vue'
    import SvgIcon from '@/components/SvgIcon'// svg component
    
    // register globally
    Vue.component('svg-icon', SvgIcon)
    
    const req = require.context('./svg', false, /\.svg$/)
    const requireAll = requireContext => requireContext.keys().map(requireContext)
    requireAll(req)


这里使用了webpack的requirecontext来读取icons/svg目录下的多个icon，否则需要一行行手动引入。

**1.解决方式一**：require.context相关代码删除，然后手动一个个引入import testIcon from "./svg/test.svg"，但是太过于繁琐，增加好多代码量。

**2.解决方式二：使用vite插件**@originjs/vite-plugin-require-context，让vite可以使用识别require.context

    import { defineConfig } from 'vite'
    import ViteRequireContext from '@originjs/vite-plugin-require-context'
    
    export default defineConfig({
      plugins: [
        createVuePlugin({ jsx: true }),
        ViteRequireContext(/* options */)
        ...	...
      ]
    }


### 9.svgo图标无法显示

切换vite前，正常显示：

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/d49a8fec-d6d3-431f-b08f-5eb329e3d849.png) 

使用vite后图标不见了：

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/f25ee98a-2f91-49f8-a7b3-86976f34e0f9.png)

这里是我解决require和require.context之后正常加载时的页面，这个两个问题如果没有解决的话，页面是直接进入不了的。所以前提是把这两个问题解决了才行。

打开控制台发现js并没有报错，只是svg没有成功显示的问题。

    <div data-v-4441ce11="">
      <div data-v-4441ce11="" class="el-tooltip icon-item" aria-describedby="el-tooltip-8235" tabindex="0">
        <svg data-v-c8a70580="" data-v-4441ce11="" aria-hidden="true" class="svg-icon disabled">
          <use data-v-c8a70580="" xlink:href="#icon-404"></use>
        </svg>
        <span data-v-4441ce11="">404</span>
      </div>
    </div>

可以看到这里使用了[svg<use> - SVG: Scalable Vector Graphics | MDN (mozilla.org)](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/use)

搜索_svg:use vite，_找到解决方案[在vite中使用svg(vue) - 掘金 (juejin.cn)](https://juejin.cn/post/7036949769842851854)：

**借助vite插件****vite-plugin-svg-icons****使vite可以完美支持svg:use:**

    import { defineConfig } from 'vite'
    import { createSvgIconsPlugin } from 'vite-plugin-svg-icons'
    
    export default defineConfig({
      plugins: [
        createVuePlugin({ jsx: true }),
        createSvgIconsPlugin({
          iconDirs: [path.resolve(__dirname, './src/icons/svg')],
          symbolId: 'icon-[dir]-[name]'
        })
        ...	...
      ]
    }
    )


### 10.vite无法识别scss变量导入

报错error和代码：

    [plugin:vite:import-analysis] Cannot read properties of undefined (reading 'url')
    D:/work/gitlab/admin-web/src/styles/element-variables.scss
    
    import variables from '@/styles/element-variables.scss'
    @import "~element-ui/packages/theme-chalk/src/index";

**这里需要修改为\*.module.scss**[功能 | Vite 官方中文文档 (vitejs.dev)](https://cn.vitejs.dev/guide/features.html#css)

> 其实在解决这个问题之前，页面的主题配置还有一些和变量相关的样式都是混乱的，修复这个问题之后瞬间一片清明！！！

### 11.videojs报错导致Maximum call stack size exceeded

1.使用vite之后videojs-contrib-hls（用来播放m3u8视频的videojs插件）报错：

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/3c878394-1820-43b6-b28d-ca4c4e47e5dd.png)

2.注释掉videojs-contrib-hls，只要打开引入了videojs组件的页面就卡住了：

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/6d1b63c1-d29d-4a4b-a75b-c5e9898ec999.png)

3.然后我尝试使用CDN的方式引入，视频可以成功播放了，但是还是有error

![image](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/f4a14d73-5288-4d86-b763-caf9141da1f4.png)

index.html增加cdn

    <!DOCTYPE html>
    <html>
    <head>
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
      <meta name="renderer" content="webkit">
      <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
      <link rel="icon" href="/favicon.ico" />
      <!-- 新增 -->
      <link href="https://cdnjs.cloudflare.com/ajax/libs/video.js/7.21.1/alt/video-js-cdn.min.css" rel="stylesheet" />
      <title>melon admin</title>
    </head>
    <body>
    <div id="app"></div>
    <!-- built files will be auto injected -->
    <script type="module" src="/src/main.js"></script>
<pre><code> &lt;!-- 新增 --&gt;
&lt;script src=&quot;https://cdnjs.cloudflare.com/ajax/libs/video.js/7.21.1/video.min.js&quot;&gt;&lt;/script&gt;
 &lt;!-- 新增 --&gt;
&lt;script src=&quot;https://cdnjs.cloudflare.com/ajax/libs/videojs-contrib-hls/5.15.0/videojs-contrib-hls.min.js&quot;&gt;&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>VideoPlay.vue</p>
<pre><code>&lt;template&gt;
  &lt;div&gt;
    &lt;div class=&quot;VideoPlay&quot;&gt;
      &lt;video
        :id=&quot;`video-${videoId}`&quot;
        ref=&quot;videoRef&quot;
        class=&quot;video-js vjs-default-skin vjs-big-play-centered&quot;
        muted
        plays-inline
        controls
        preload=&quot;auto&quot;
        height=&quot;400px&quot;
        width=&quot;100%&quot;
      /&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/template&gt;

&lt;script&gt;
// 注释掉videojs npm导入
// import videojs from 'video.js'
// import 'video.js/dist/video-js.css'
// import 'videojs-contrib-hls'

const config = {
  bigPlayButton: true,
  textTrackDisplay: false,
  posterImage: true,
  errorDisplay: false,
  controlBar: {
    currentTimeDisplay: true, // 当前时间
    timeDivider: true, // 时间分割线
    durationDisplay: true, // 总时间
    progressControl: true, // 进度条
    remainingTimeDisplay: {
      displayNegative: true
    }, //
    fullscreenToggle: true // 全屏按钮
  },
  playbackRates: [0.8, 1, 1.25, 1.5, 2],
  html5: {
    vhs: {
      withCredentials: false
    }
  }
}

export default {
  name: 'VideoPlay',
  props: {
    videoId: {
      default: Math.random().toString().slice(2, 6),
      type: [String, Number]
    },
    videoSrc: {
      default: '',
      type: String
    }
  },
  data() {
    return {
      videoPlayerInstance: null,
    }
  },
  mounted() {
    try {
      this.videoSrc &amp;&amp; this.getVideoPlayInstance()
    } catch (e) {
      console.error('init video')
    }
  },
  destroyed() {
    try {
      this.videoPlayerInstance &amp;&amp; this.videoPlayerInstance.dispose()
    } catch (e) {
      console.error('destroyed video', e)
    }
  },
  methods: {
    setVideo(id, source) {
      try {
        config.sources = [{
          type: 'application/x-mpegURL',
          src: source
        }]
        // const instance = videojs(
        // 使用全局window.videojs
        const instance = window.videojs(
          id,
          config,
          () =&gt; {
            instance.play()
          })
        return instance
      } catch (e) {
        console.error('setVideo', e)
      }
    },
    getVideoPlayInstance() {
      if (!this.videoPlayerInstance) {
        this.videoPlayerInstance = this.setVideo(`video-${this.videoId}`, this.videoSrc)
      }
    }
  }
}
&lt;/script&gt;

&lt;style lang=&quot;scss&quot; scoped&gt;
.VideoPlay {
  width: 100%;
  height: 500px;

  .video-js {
    width: 100%;
    height: 100%;
  }
}

.audioPlay {
  width: 100%;
  height: 30px;

  .video-js {
    width: 100%;
    height: 100%;
    background: transparent;
  }
}
&lt;/style
</code></pre>
<p>4.解决第3步的报错，切换了其他版本还是有这个错误，于是打算把<a href="https://github.com/videojs/videojs-contrib-hls">videojs-contrib-hls</a>源码clone一份看看报错的地方能不能修改下源码，发现<a href="https://github.com/videojs/videojs-contrib-hls#notice-this-project-will-be-deprecated-and-is-succeeded-by-videojs-http-streaming-vhs-supports-hls-and-dash-and-is-built-into-videojs-7-see-the-videojs-7-blog-post">README.md</a>：</p>
<figure data-type="image" tabindex="6"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/1c43e243-baff-4e38-b263-21d1aac2b639.png" alt="image" loading="lazy"></figure>
<p><a href="https://videojs.com/blog/video-js-7-is-here/">Video.js Blog | Video.js (videojs.com)</a>博客显示videojs7使用<a href="https://github.com/videojs/http-streaming">videojs/http-streaming</a>插件来替代<a href="https://github.com/videojs/videojs-contrib-hls">videojs-contrib-hls</a>。</p>
<blockquote>
<p><a href="https://github.com/videojs/http-streaming">videojs/http-streaming</a>: HLS, DASH, and future HTTP streaming protocols library for video.js</p>
</blockquote>
<p>刚巧项目中的videojs也是7.x版本，于是我切换了<a href="https://github.com/videojs/http-streaming">videojs/http-streaming</a>CDN,看了<a href="https://jsbin.com/gejugat/edit?html,output">官方DEMO</a>,需要把标签video替换为video-js，成功解决error，但是多了一个warning， 问题不大</p>
<figure data-type="image" tabindex="7"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/a78f196d-97ad-43f1-acd2-f5c0a7c7c081.png" alt="image" loading="lazy"></figure>
<p>这个就先忍了吧<sup>_</sup></p>
<figure data-type="image" tabindex="8"><img src="https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/W4j6OJVBQVpgO3p8/img/172e2ad1-bdee-4809-8ba2-09605177569f.png" alt="image" loading="lazy"></figure>
<p>最终的index.html</p>
<pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;meta charset=&quot;utf-8&quot;&gt;
    &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge,chrome=1&quot;&gt;
    &lt;meta name=&quot;renderer&quot; content=&quot;webkit&quot;&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no&quot;&gt;
    &lt;link rel=&quot;icon&quot; href=&quot;/favicon.ico&quot; /&gt;
    &lt;link href=&quot;https://unpkg.com/video.js/dist/video-js.css&quot; rel=&quot;stylesheet&quot;&gt;
    &lt;title&gt;melon admin&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;div id=&quot;app&quot;&gt;&lt;/div&gt;
    &lt;!-- built files will be auto injected --&gt;
    &lt;script type=&quot;module&quot; src=&quot;/src/main.js&quot;&gt;&lt;/script&gt;
    &lt;script src=&quot;https://unpkg.com/video.js/dist/video.js&quot;&gt;&lt;/script&gt;
    &lt;script src=&quot;https://unpkg.com/@videojs/http-streaming/dist/videojs-http-streaming.js&quot;&gt;&lt;/script&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<blockquote>
<p>在解决这个问题之前，我内心是崩溃的，找了诸多方式都无法解决，就差一点儿就放弃了，行百里者半九十，我坚持找解决方案，最终终于成功了，点个赞！</p>
</blockquote>
<blockquote>
<p>为了渐进升级，目前保留`vue-cli-service`和`vite`共存，两者都可以使用，后续可以把`vue-cli-service`相关的依赖和代码删除即可</p>
</blockquote>
<h2 id="6参考文档">6.参考文档</h2>
<ul>
<li>
<p><a href="https://vitejs.cn/">Home | Vite中文网 (vitejs.cn)</a></p>
</li>
<li>
<p><a href="https://originjs.org/">Origin.js- Webpack 转 Vite 的转换工具</a>： 这个是我在切换vite之后发现的一个宝藏项目，可以让我们少走很多弯路。</p>
</li>
<li>
<p><a href="https://cdnjs.com/libraries">cdnjs.com-CDN</a></p>
</li>
<li>
<p><a href="https://videojs.com/blog/video-js-7-is-here/">Video.js Blog | Video.js (videojs.com)</a></p>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[25、反射应用]]></title>
        <id>https://keekuun.github.io/post/25fan-she-ying-yong/</id>
        <link href="https://keekuun.github.io/post/25fan-she-ying-yong/">
        </link>
        <updated>2023-01-04T09:09:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-反射的使用">一 反射的使用</h2>
<h4 id="11-反射操作简单数据类型">1.1 反射操作简单数据类型</h4>
<pre><code class="language-go">	var num int64 = 100

	// 设置值：指针传递
	ptrValue := reflect.ValueOf(&amp;num)
	newValue := ptrValue.Elem()                         // Elem()用于获取原始值的反射对象
	fmt.Println(&quot;type：&quot;, newValue.Type())				// int64
	fmt.Println(&quot; can set：&quot;, newValue.CanSet())		// true
	newValue.SetInt(200)

	// 获取值：值传递
	rValue := reflect.ValueOf(num)
	fmt.Println(rValue.Int())					// 方式一：200
	fmt.Println(rValue.Interface().(int64))		// 方式二：200
</code></pre>
<h4 id="12-反射进行类型推断">1.2 反射进行类型推断</h4>
<pre><code class="language-go">	type user struct {
		Name string
		Age int
	}

	u := &amp;user{
		Name: &quot;Ruyue&quot;,
		Age:  100,
	}

	fmt.Println(reflect.TypeOf(u))			// *main.user
	fmt.Println(reflect.TypeOf(*u))			// main.user
	fmt.Println(reflect.TypeOf(*u).Name())	// user
	fmt.Println(reflect.TypeOf(*u).Kind())	// struct
</code></pre>
<h4 id="13-反射操作指针">1.3 反射操作指针</h4>
<pre><code class="language-go">	type user struct {
		Name string
		Age int
	}

	u := &amp;user{
		Name: &quot;Ruyue&quot;,
		Age:  100,
	}

	typeOfUser = reflect.TypeOf(u).Elem()
	fmt.Println(&quot;element name: &quot;, typeOfUser.Name())		// user
	fmt.Println(&quot;element kind: &quot;, typeOfUser.Kind())		// struct
</code></pre>
<h4 id="14-反射操作结构体">1.4 反射操作结构体</h4>
<p>反射可以获取结构体的详细信息：</p>
<pre><code class="language-go">	type user struct {
		Name string
		Age  int `json:&quot;age&quot; id:&quot;100&quot;` // 结构体标签
	}

	s := user{
		Name: &quot;zs&quot;,
		Age:  1,
	}

	typeOfUser := reflect.TypeOf(s)

	// 字段用法
	for i := 0; i &lt; typeOfUser.NumField(); i++ {        // NumField 当前结构体有多少个字段
		fieldType := typeOfUser.Field(i)                // 获取每个字段
		fmt.Println(fieldType.Name, fieldType.Tag)
	}
	if userAge, ok := typeOfUser.FieldByName(&quot;Age&quot;); ok {
		fmt.Println(userAge)						// {Age  int json:&quot;age&quot; id:&quot;100&quot; 16 [1] false}
	}

	// 方法用法
	for i := 0; i &lt; typeOfUser.NumMethod(); i++ {
		fieldType := typeOfUser.Method(i)                // 获取每个字段
		fmt.Println(fieldType.Name)
	}
</code></pre>
<h2 id="二-反射调用函数与方法">二 反射调用函数与方法</h2>
<h4 id="21-使用反射调用函数">2.1 使用反射调用函数</h4>
<p>如果反射值对象(reflect.Value)中值的类型为函数时，可以通过 reflect.Value调用该 函数。使用反射调用函数时，需要将参数使用反射值对象的切片 口reflect.Value 构造后传入 Call()方法中 ， 调用完成时，函数的返回值通过 []reflect.Value 返回 。</p>
<pre><code class="language-go">func add(name string, age int) {
	fmt.Printf(&quot;name is %s, age is %d \n&quot;, name, age)
}

func main() {
	funcValue := reflect.ValueOf(add)
	params := []reflect.Value{reflect.ValueOf(&quot;lisi&quot;), reflect.ValueOf(20)}

	reList := funcValue.Call(params)
	fmt.Println(reList)			// 函数返回值
}
</code></pre>
<h3 id="22-反射调用方法">2.2 反射调用方法</h3>
<p>方法的调用是需要接收者的：</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
	&quot;reflect&quot;
)

type user struct {
	Name string
	Age int
}
func (u *user) ShowName() {
	fmt.Println(u.Name)
}

func (u *user) AddAge(addNum int) {
	fmt.Println(&quot;age add result:&quot;, u.Age + addNum)
}

func main() {

	u := &amp;user{&quot;lisi&quot;, 20}
	v := reflect.ValueOf(u)

	// 调用无参方法
	methodV := v.MethodByName(&quot;ShowName&quot;)
	methodV.Call(nil)		// 或者传递一个空切片也可

	// 调用有参方法
	methodV2 := v.MethodByName(&quot;AddAge&quot;)
	args := []reflect.Value{reflect.ValueOf(30)}	//
	methodV2.Call(args)
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[24、反射概述]]></title>
        <id>https://keekuun.github.io/post/24fan-she-gai-shu/</id>
        <link href="https://keekuun.github.io/post/24fan-she-gai-shu/">
        </link>
        <updated>2023-01-04T09:09:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-反射简介">一 反射简介</h2>
<p>反射是指在程序运行期对程序本身进行访问和修改的能力，即可以在运行时动态获取变量的各种信息，比如变量的类型（type），类别（kind），如果是结构体变量，还可以获取到结构体本身的信息（字段与方法），通过反射，还可以修改变量的值，可以调用关联的方法。</p>
<p>反射常用在框架的开发上，一些常见的案例，如JSON序列化时候tag标签的产生，适配器函数的制作等，都需要用到反射。反射的两个常见使用场景：</p>
<ul>
<li>不知道函数的参数类型：没有约定好参数、传入类型很多，此时类型不能统一表示，需要反射</li>
<li>不知道调用哪个函数：比如根据用户的输入来决定调用特定函数，此时需要依据函数、函数参数进行反射，在运行期间动态执行函数</li>
</ul>
<p>Go程序的反射系统无法获取到一个可执行文件空间中或者是一个包中的所有类型信息，需要配合使用标准库中对应的词法、语法解析器和抽象语法树( AST) 对源码进行扫描后获得这些信息。</p>
<p>贴士：</p>
<ul>
<li>C，C++没有支持反射功能，只能通过 typeid 提供非常弱化的程序运行时类型信息。</li>
<li>Java、 C#等语言都支持完整的反射功能。</li>
<li>Lua、JavaScript类动态语言，由于其本身的语法特性就可以让代码在运行期访问程序自身的值和类型信息，因此不需要反射系统。</li>
</ul>
<p>注意：</p>
<ul>
<li>在编译期间，无法对反射代码进行一些错误提示。</li>
<li>反射影响性能</li>
</ul>
<h2 id="二-反射是如何实现的">二 反射是如何实现的</h2>
<p>反射是通过接口的类型信息实现的，即反射建立在类型的基础上：当向接口变量赋予一个实体类型的时候，接口会存储实体的类型信息。</p>
<p>Go中反射相关的包是<code>reflect</code>，在该包中，定义了各种类型，实现了反射的各种函数，通过它们可以在运行时检测类型的信息、改变类型的值。</p>
<p>变量包括type、value两个部分（所以 <code>nil != nil</code> ），type包括两部分：</p>
<ul>
<li>static type：在开发时使用的类型，如int、string</li>
<li>concrete type：是runtime系统使用的类型</li>
</ul>
<p>类型能够断言成功，取决于 concrete type ，如果一个reader变量，如果 concrete type 实现了 write 方法，那么它可以被类型断言为writer。</p>
<p>Go中，反射与interface类型相关，其type是 concrete type，只有interface才有反射！每个interface变量都有一个对应的pair，pair中记录了变量的实际值和类型（value, type）。即一个接口类型变量包含2个指针，一个指向对应的 concrete type ，另一个指向实际的值 value。</p>
<p>示例：</p>
<pre><code class="language-go">var r io.Reader				// 定义了一个接口类型
r, err := os.OpenFile()		// 记录接口的实际类型、实际值

var w io.Writer				// 定义一个接口类型
w = r.(io.Writer)			// 赋值时，接口内部的pair不变，所以 w 和 r 是同一类型
</code></pre>
<h2 id="三-go中反射初识">三 Go中反射初识</h2>
<h3 id="31-reflect包的两个函数">3.1 reflect包的两个函数</h3>
<p>reflect 提供了2个重要函数：</p>
<ul>
<li>ValueOf()：获取变量的值，即pair中的 value</li>
<li>TypeOf()：获取变量的类型，即pair中的  concrete type</li>
</ul>
<pre><code class="language-go">	type Person struct {
		Name string
		Age int
	}
	p := Person{ &quot;lisi&quot;, 13}

	fmt.Println(reflect.ValueOf(p))			// {lisi 13}  变量的值
	fmt.Println(reflect.ValueOf(p).Type())	// main.Person 变量类型的对象名

	fmt.Println(reflect.TypeOf(p))			//  main.Person	变量类型的对象名

	fmt.Println(reflect.TypeOf(p).Name())	// Person:变量类型对象的类型名
	fmt.Println(reflect.TypeOf(p).Kind())	// struct:变量类型对象的种类名

	fmt.Println(reflect.TypeOf(p).Name() == &quot;Person&quot;)	// true
	fmt.Println(reflect.TypeOf(p).Kind() == reflect.Struct)	//true
</code></pre>
<p>类型与种类的区别：</p>
<ul>
<li>Type是原生数据类型： int、string、bool、float32 ，以及 type 定义的类型，对应的反射获取方法是 reflect.Type 中 的 Name()</li>
<li>Kind是对象归属的品种：Int、Bool、Float32、Chan、String、Struct、Ptr（指针）、Map、Interface、Fune、Array、Slice、Unsafe Pointer等</li>
</ul>
<h3 id="32-静态类型与动态类型">3.2 静态类型与动态类型</h3>
<p>静态类型：变量声明时候赋予的类型</p>
<pre><code class="language-go">	type MyInt int			// int 是静态类型
	var i *int				// *int 是静态类型
</code></pre>
<p>动态类型：运行时给这个变量赋值时，这个值的类型即为动态类型（为nil时没有动态类型）。</p>
<pre><code class="language-go">	var A interface{}		// 空接口 是静态类型，必须是接口类型才能实现类型动态变化
	A = 10					// 此时静态类型为 interface{} 动态为int
	A = &quot;hello&quot;				// 此时静态类型为 interface{} 动态为string
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[23、时间操作]]></title>
        <id>https://keekuun.github.io/post/23shi-jian-cao-zuo/</id>
        <link href="https://keekuun.github.io/post/23shi-jian-cao-zuo/">
        </link>
        <updated>2023-01-04T09:08:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-时间操作">一 时间操作</h2>
<h3 id="11-创建时间">1.1 创建时间</h3>
<p>Golang中时间操作位于 time 包中，常见操作有：</p>
<pre><code class="language-go">	// 当前时间
	nowTime := time.Now()
	fmt.Printf(&quot;当前时间为：%T\n&quot;, nowTime)	// 其类型是 time.Time
	fmt.Println(nowTime)							// 2019-01-01 13:50:07.522712 +0800 CST m=+0.000138178

	// 自定义时间
	customTime := time.Date(2008, 7, 15, 13, 30,0,0, time.Local)
	fmt.Println(customTime)							// 2008-07-15 13:30:00 +0800 CST
</code></pre>
<h3 id="12-时间格式化与解析">1.2 时间格式化与解析</h3>
<p>Go的时间格式化必须传入Go的生日：<code>Mon Jan 2 15:04:05 -0700 MST 2006</code></p>
<pre><code class="language-go">	nowTime := time.Now()
	stringTime := nowTime.Format(&quot;2006年1月2日 15:04:05&quot;)
	fmt.Println(stringTime)			// 2019年1月1日 13:55:30
</code></pre>
<p>Go的时间解析：</p>
<pre><code class="language-go">    stringTime := &quot;2019-01-01 15:03:01&quot;
	objTime,_ := time.Parse(&quot;2006-01-02 15:04:05&quot;,stringTime)
	fmt.Println(objTime)		// 2019-01-01 15:03:01 +0000 UTC
</code></pre>
<p>注意：这些方法的参数模板必须与时间一一对应，否则报错！</p>
<h3 id="13-获取-年-月-日">1.3 获取 年 月 日</h3>
<pre><code class="language-go">    nowTime := time.Now()

	year, month, day := nowTime.Date()
	fmt.Println(year, month, day)		// 2019 November 01

	hour, min, sec := nowTime.Clock()
	fmt.Println(hour, min, sec)

	fmt.Println(nowTime.Year())
	fmt.Println(nowTime.Month())
	fmt.Println(nowTime.Hour())

	fmt.Println(nowTime.YearDay())		// 指今年一共过了多少天
</code></pre>
<h3 id="14-时间戳">1.4 时间戳</h3>
<p>时间戳是指计算时间距离 1970年1月1日的秒数：</p>
<pre><code class="language-go">	nowTime := time.Now()
	fmt.Println(nowTime.Unix())
</code></pre>
<h3 id="15-时间间隔">1.5 时间间隔</h3>
<pre><code class="language-go">	nowTime := time.Now()
	fmt.Println(nowTime.Add(time.Second * 10))  				// 10秒后
    fmt.Println(nowTime.AddDate(1, 0, 0))	// 1年后
</code></pre>
<p>贴士：</p>
<ul>
<li>传入负数则是往前计算</li>
<li>Sub()函数可以用来计算两个时间的差值</li>
</ul>
<h3 id="16-时间睡眠">1.6 时间睡眠</h3>
<pre><code class="language-go">	time.Sleep(time.Second * 3)	// 程序睡眠三秒钟
</code></pre>
<h2 id="二-时间中的通道操作定时器">二 时间中的通道操作（定时器）</h2>
<p>标准库中的Timer可以让用户自定义一个定时器，在对用select处理多个channel的超时、单channel读写的超时等情形时很方便：</p>
<pre><code class="language-go">	timer := time.NewTimer(time.Second * 3)		// 类型为 *time.Timer
	ch := timer.C								// timer内部包含一个通道
	fmt.Println(&lt;-ch)							// 3秒后，通道内有了数据，可以取出
</code></pre>
<p>配合协程：</p>
<pre><code class="language-go">
	timer := time.NewTimer(time.Second * 3)		// 类型为 *time.Timer
	go func() {
		&lt;- timer.C
		fmt.Println(&quot;timer 结束&quot;)
	}()

	time.Sleep(time.Second * 5)
	flag := timer.Stop()			// 取消定时器
    fmt.Println(flag)				// false
</code></pre>
<p>time.After函数的使用：</p>
<pre><code class="language-go">	ch := time.After(time.Second * 3)		// 底层其实是 new Timer(d).C
	newTime := &lt;-ch			// 阻塞3秒
	fmt.Println(newTime)
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[22、文件操作-读]]></title>
        <id>https://keekuun.github.io/post/22wen-jian-cao-zuo-du/</id>
        <link href="https://keekuun.github.io/post/22wen-jian-cao-zuo-du/">
        </link>
        <updated>2023-01-04T09:08:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-文件读取">一 文件读取</h2>
<p>文件读写的接口位于io包，file文件类是这些接口的实现类。</p>
<h3 id="11-直接读取-read">1.1 直接读取 read()</h3>
<p>read() 实现的是按字节数读取：</p>
<pre><code class="language-go">	readByte := make([]byte, 128)			// 指定要读取的长度
	for {
		n, err := f.Read(readByte)			// 将数据读取如切片，返回值 n 是实际读取到的字节数
		if err != nil &amp;&amp; err != io.EOF{		// 如果读到了文件末尾：EOF 即 end of file
			fmt.Println(&quot;read file : &quot;, err)
			break
		}

		fmt.Println(&quot;read: &quot;, string(readByte[:n]))
		if n &lt; 128 {
			fmt.Println(&quot;read end&quot;)
			break
		}
	}
</code></pre>
<h3 id="12-bufio的写操作">1.2 bufio的写操作</h3>
<p>bufio封装了io.Reader、io.Writer接口对象，并创建了另一个也实现了该接口的对象：bufio.Reader、bufio.Writer。通过该实现，bufio实现了文件的缓冲区设计，可以大大提高文件I/O的效率。</p>
<p>使用bufio读取文件时，先将数据读入内存的缓冲区（缓冲区一般比要比程序中设置的文件接收对象要大），这样就可以有效降低直接I/O的次数。</p>
<p><code>bufio.Read([]byte)</code>相当于读取大小<code>len(p)</code>的内容：</p>
<ul>
<li>当缓冲区有内容时，将缓冲区内容全部填入p并清空缓冲区</li>
<li>当缓冲区没有内容且<code>len(p)&gt;len(buf)</code>，即要读取的内容比缓冲区还要大，直接去文件读取即可</li>
<li>当缓冲区没有内容且<code>len(p)&lt;len(buf)</code>，即要读取的内容比缓冲区小，读取文件内容并填满缓冲区，并将p填满</li>
<li>以后再次读取时，缓冲区有内容，将缓冲区内容全部填入p并清空缓冲区（和第一步一致）</li>
</ul>
<p>示例：</p>
<pre><code class="language-go">	// 创建读对象
	reader := bufio.NewReader(f)

	// 读一行数据
	byt, _ := reader.ReadBytes('\n')			
	fmt.Println(string(byt))
</code></pre>
<p>ReadString() 函数也具有同样的功能，且能直接读取到字符串数据，无需转换，示例：读取大文件的全部数据</p>
<pre><code class="language-go">	reader := bufio.NewReader(f)
	for {										// 按照缓冲区读取：读取到特定字符结束
		str, err := reader.ReadString('\n')		// 按行读取
		if err != nil &amp;&amp; err != io.EOF {
			fmt.Println(&quot;read err: &quot;, err)
			break
		}
		fmt.Println(&quot;str = &quot;, str)
		if err == io.EOF {
			fmt.Print(&quot;read end&quot;)
			break
		}
	}
</code></pre>
<p>在Unix设计思想中，一切皆文件，命令行输入也可以作为文件读入：</p>
<pre><code class="language-go">	reader := bufio.NewReader(os.Stdin)
	s, _ := reader.ReadString(&quot;-&quot;)		// 假设命令行以 - 开始
</code></pre>
<p>缓冲的思想：通过bufio，数据被写入用户缓冲，再进入系统缓冲，最后由操作系统将系统缓冲区的数据写入磁盘。</p>
<h3 id="13-ioioutil-包文件读取">1.3 io/ioutil 包文件读取</h3>
<p>ioutil直接读取文件：</p>
<pre><code class="language-go">	ret, err := ioutil.ReadFile(&quot;test.txt&quot;)
	if err != nil {
		fmt.Println(&quot;read err :&quot;, err)
		return
	}
	fmt.Println(string(ret))
</code></pre>
<h3 id="二-文件写入">二 文件写入</h3>
<h3 id="21-直接写">2.1 直接写</h3>
<pre><code class="language-go">	f, err := os.OpenFile(&quot;test.txt&quot;, os.O_CREATE | os.O_WRONLY, os.ModePerm)
	if err != nil {
		fmt.Println(&quot;open err:&quot;, err)
		return
	}
	defer f.Close()

	n, err := f.Write([]byte(&quot;hello world&quot;))
	if err != nil {
		fmt.Println(&quot;write err:&quot;, err)
	}
	fmt.Println(n)								// 每次都会从头开始重新写入
</code></pre>
<p>上述案例中，如果我们不想每次写入都会从头开始重新写入，那么需要将打开模式修改为：<code>os.O_CREATE | os.O_WRONLY | os.O_APPEND</code></p>
<h3 id="22-bufio的写操作">2.2 bufio的写操作</h3>
<pre><code class="language-go">	writer := bufio.NewWriter(f)
	_, err = writer.WriteString(&quot;hello world!&quot;)
	if err != nil {
		fmt.Println(&quot;write err:&quot;, err)
		return
	}
	writer.Flush()		// 必须刷新缓冲区：将缓冲区的内容写入文件中。如果不刷新，则只会在内容超出缓冲区大小时写入
</code></pre>
<h3 id="23-ioioutil-包文件写入">2.3  io/ioutil 包文件写入</h3>
<pre><code class="language-go">	s := &quot;你好世界&quot;
	err := ioutil.WriteFile(&quot;test.txt&quot;, []byte(s), os.ModePerm)
</code></pre>
<h2 id="三-文件读取偏移量">三 文件读取偏移量</h2>
<p>文件读取时，是可以控制光标位置的：</p>
<pre><code class="language-go">	f, err := os.OpenFile(&quot;test.txt&quot;, os.O_RDWR, os.ModePerm)
	if err != nil {
		fmt.Println(&quot;open err:&quot;, err)
		return
	}
	defer f.Close()

	// 读取前五个字节，假设读取的文件内容为： hello world!
	bs := []byte{0}		// 创建1个字节的切片
	_, err = f.Read(bs)
	if err != nil {
		fmt.Println(&quot;read err:&quot;, err)
		return
	}
	fmt.Println(&quot;读到的数据是：&quot;, string(bs))	// h

	// 移动光标
	_, err = f.Seek(4, io.SeekStart)		// 光标从开始位置(h之前)，移动4位，到达o之前
	if err != nil {
		fmt.Println(&quot;seek err:&quot;, err)
		return
	}
	_, err = f.Read(bs)
	if err != nil {
		fmt.Println(&quot;read err:&quot;, err)
		return
	}
	fmt.Println(&quot;读到的数据是：&quot;, string(bs))		// o
</code></pre>
<p>通过记录光标的位置，可以实现断点续传：假设已经下载了1KB文件，即本地临时文件存储了1KB，此时断电，重启后通过本地文件大小、Seek()方法获取到上次读取文件的光标位置即可实现继续下载！</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[21、文件操作-写]]></title>
        <id>https://keekuun.github.io/post/21wen-jian-cao-zuo-xie/</id>
        <link href="https://keekuun.github.io/post/21wen-jian-cao-zuo-xie/">
        </link>
        <updated>2023-01-04T09:08:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-文件的基本操作">一 文件的基本操作</h2>
<h3 id="11-创建文件">1.1 创建文件</h3>
<pre><code class="language-go">	f, err := os.Create(&quot;test.txt&quot;)
	if err != nil {
		fmt.Println(err)
		return
	}
	fmt.Println(f)				// 打印文件指针
	f.Close()					// 打开的资源在不使用时必须关闭
</code></pre>
<p>使用Create()创建文件时：</p>
<ul>
<li>如果文件不存在，则创建文件。</li>
<li>如果文件存在，则清空文件内内容。</li>
<li>Create创建的文件任何人都可以读写。</li>
</ul>
<h3 id="12-打开文件写入内容">1.2 打开文件，写入内容</h3>
<p>打开文件有两种方式：</p>
<ul>
<li>Open()：以只读的方式打开文件，若文件不存在则会打开失败</li>
<li>OpenFile()：打开文件时，可以传入打开方式，该函数的三个参数：
<ul>
<li>参数1：要打开的文件路径</li>
<li>参数2：文件打开模式，如 <code>O_RDONLY</code>，<code>O_WRONGLY</code>，<code>O_RDWR</code>，还可以通过管道符来指定文件不存在时创建文件</li>
<li>参数3：文件创建时候的权限级别，在0-7之间，常用参数为6</li>
</ul>
</li>
</ul>
<pre><code class="language-go">	f, err := os.OpenFile(&quot;test.txt&quot;, os.O_APPEND | os.O_RDWR, os.ModeAppend)
	if err != nil {
		fmt.Println(&quot;open file err: &quot;, err)
		return
	}
	f.Close()
</code></pre>
<p>常用的文件打开模式：</p>
<pre><code class="language-go">	O_RDONLY 	int = syscall.O_RDONLY		// 只读
	O_WRONGLY	int = syscall.O_WRONGLY		// 只写
	O_RDWR 		int = syscall.O_RDWR		// 读写
	O_APPEND 	int = syscall.O_APPEND		// 写操作时将数据追加到文件末尾
	O_CREATE 	int = syscall.O_CREATE		// 如果不存在则创建一个新文件
	O_EXCL 		int = syscall.O_EXCL		// 打开文件用于同步I/O
	O_TRUNC		int = syscall.O_TRUNC		// 如果可能，打开时清空文件
</code></pre>
<h3 id="13-写文件">1.3 写文件</h3>
<p>写入字节 <code>Write()</code>：</p>
<pre><code class="language-go">	// 写入文件内容
	n, err := f.Write([]byte(&quot;hello&quot;))
	if err != nil {
		fmt.Println(&quot;write err: &quot;, err)
		return
	}
	fmt.Println(&quot;write number = &quot;, n)
</code></pre>
<p>按字符串写 <code>WriteString()</code>：</p>
<pre><code class="language-go">	// 写入文件内容
	n, err := f.WriteString([&quot;hello&quot;)		// 会将前5个字符替换为 hello
	if err != nil {
		fmt.Println(&quot;write err: &quot;, err)
		return
	}
	fmt.Println(&quot;write number = &quot;, n)
</code></pre>
<p>修改文件的读写指针位置 <code>Seek()</code>，包含两个参数：</p>
<ul>
<li>参数1：偏移量，为正数时向文件尾偏移，为负数时向文件头偏移</li>
<li>参数2：偏移的开始位置，包括：
<ul>
<li>io.SeekStart：从文件起始位置开始</li>
<li>io.SeekCurrent：从文件当前位置开始</li>
<li>io.SeekEnd：从文件末尾位置开始</li>
</ul>
</li>
</ul>
<p><code>Seek()</code>函数返回</p>
<pre><code class="language-go">	f, _ := os.OpenFile(&quot;test.txt&quot;,os.O_RDWR, 6)
	off, _ := f.Seek(5, io.SeekStart)
	fmt.Println(off)							// 5
	n, _ := f.WriteAt([]byte(&quot;111&quot;), off)
	fmt.Println(n)
	f.Close()
</code></pre>
<h3 id="14-获取文件描述信息-osstat">1.4 获取文件描述信息 os.Stat()</h3>
<p>Go的os包中定义了file类，封装了文件描述信息，同时也提供了Read、Write的实现。</p>
<pre><code class="language-go">	fileInfo, err := os.Stat(&quot;./test.txt&quot;)
	if err != nil {
		fmt.Println(&quot;stat err: &quot;, err)
		return
	}
	fmt.Printf(&quot;%T\n&quot;, fileInfo)		// *os.fileStat
</code></pre>
<p>获取到的fileInfo内部包含 <code>文件名Name()</code>、<code>大小Size()</code>、<code>是否是目录IsDir()</code> 等操作。</p>
<h3 id="15-路径-目录操作">1.5 路径、目录操作</h3>
<pre><code class="language-go">	// 路径操作
	fmt.Println(filepath.IsAbs(&quot;./test.txt&quot;))	// false：判断是否是绝对路径
	fmt.Println(filepath.Abs(&quot;./test.txt&quot;))		// 转换为绝对路径

	// 创建目录
	err := os.Mkdir(&quot;./test&quot;, os.ModePerm)
	if err != nil {
		fmt.Println(&quot;mkdir err: &quot;, err)
		return
	}

	// 创建多级目录
	err = os.MkdirAll(&quot;./dd/rr&quot;, os.ModePerm)
	if err != nil {
		fmt.Println(&quot;mkdirAll err: &quot;, err)
		return
	}
</code></pre>
<p>贴士：Openfile()可以用于打开目录。</p>
<h3 id="16-删除文件">1.6 删除文件</h3>
<pre><code class="language-go">	err := os.Remove(&quot;test.txt&quot;)
	if err != nil {
		fmt.Println(&quot;remove err:&quot;, err)
		return 
	}
</code></pre>
<p>该函数也可用于删除目录（只能删除空目录）。如果要删除非空目录，需要使用 <code>RemoveAll()</code> 函数</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[20、接口类型-断言与多态]]></title>
        <id>https://keekuun.github.io/post/20jie-kou-lei-xing-duan-yan-yu-duo-tai/</id>
        <link href="https://keekuun.github.io/post/20jie-kou-lei-xing-duan-yan-yu-duo-tai/">
        </link>
        <updated>2023-01-04T09:07:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-断言">一 断言</h2>
<p>接口是编程的规范，他也可以作为函数的参数，以让函数更具备适用性。在下列示例中，有三个接口动物接口、飞翔接口、游泳接口，两个实现类鸟类与鱼类：</p>
<ul>
<li>鸟类：实现了动物接口，飞翔接口</li>
<li>鱼类：实现了动物接口，游泳接口</li>
</ul>
<pre><code class="language-go">package main

import &quot;fmt&quot;

// 定义一个通用接口：动物接口
type Animal interface {
	Breath()			// 动物都具备 呼吸方法
}

type Flyer interface {
	Fly()
}
type Swimer interface {
	Swim()
}

// 定义一个鸟类：其呼吸的方式是在陆地
type Bird struct {
	Name string
	Food string
	Kind string
}
func (b *Bird) Breath() {
	fmt.Println(&quot;鸟 在 陆地 呼吸&quot;)
}
func (b *Bird) Fly() {
	fmt.Printf(&quot;%s 在 飞\n&quot;, b.Name)
}

// 定义一个鱼类：其呼吸方式是在水下
type Fish struct {
	Name string
	Kind string
}
func (f *Fish) Breath() {
	fmt.Println(&quot;鱼 在 水下 呼吸&quot;)
}
func (f *Fish) Swim() {
	fmt.Printf(&quot;%s 在游泳\n&quot;, f.Name)
}

// 一个普通函数，参数是动物接口
func Display(a Animal) {
	// 直接调用接口中的方法
	a.Breath()
	// 调用实现类的成员：此时会报错
	fmt.Println(a.Name)
}

func main() {
	var b = &amp;Bird{
		&quot;斑鸠&quot;,
		&quot;蚂蚱&quot;,
		&quot;鸟类&quot;
	}
	Display(b)
}
</code></pre>
<p>接口类型无法直接访问其具体实现类的成员，需要使用断言（type assertions），对接口的类型进行判断，类型断言格式：</p>
<pre><code class="language-go">t := i.(T)			//不安全写法：如果i没有完全实现T接口的方法，这个语句将会触发宕机
t, ok := i.(T)		// 安全写法：如果接口未实现接口，将会把ok掷为false，t掷为T类型的0值
</code></pre>
<ul>
<li>i代表接口变量</li>
<li>T代表转换的目标类型</li>
<li>t代表转换后的变量</li>
</ul>
<p>上述案例的Dsiplay就可以书写为：</p>
<pre><code class="language-go">func Display(a Animal) {
	// 直接调用接口中的方法
	a.Breath()
	// 调用实现类的成员：此时会报错
	instance, ok := a.(*Bird)	// 注意：这里必须是 *Bird类型，因为是*Bird实现了接口，不是Bird实现了接口
	if ok {
		// 得到了具体的实现类，才能访问实现类的成员
		fmt.Println(&quot;该鸟类的名字是：&quot;, instance.Name)
	} else {
		fmt.Println(&quot;该动物不是鸟类&quot;)
	}
}
</code></pre>
<h2 id="二-接口类型转换">二 接口类型转换</h2>
<p>在接口定义时，其类型已经确定，因为接口的本质是方法签名的集合，如果两个接口的方法签名集合相同（顺序可以不同），则这2个接口之间不需要强制类型转换就可以相互赋值，因为go编译器在校验接口是否能赋值时，比较的是二者的方法集。</p>
<p>在上一节中，函数Display接收的是Animal接口类型，在断言后转换为了别的类型：*Bird(实现类指针类型)：</p>
<pre><code class="language-go">func Display(a Animal) {
	instance, ok := a.(*Bird)		// 动物接口转换为了 *Bird实现类
	if ok {
		// 得到了具体的实现类，才能访问实现类的成员
		fmt.Println(&quot;该鸟类的名字是：&quot;, instance.Name)
	} else {
		fmt.Println(&quot;该动物不是鸟类&quot;)
	}
}
</code></pre>
<p>其实，断言还可以将接口转换成另外一个接口：</p>
<pre><code class="language-go">func Display(a Animal) {
	instance, ok := a.(Flyer)			// 动物接口转换为了飞翔接口
	if ok {
		instance.Fly()
	} else {
		fmt.Println(&quot;该动物不会飞&quot;)
	}

}
</code></pre>
<p>一个实现类往往实现了很多接口，为了精准类型查询，可以使用switch语句来判断对象类型：</p>
<pre><code class="language-go">var v1 interfaceP{} = ...
switch v := v1.(type) {
	case int:
	case string:
	...
}
</code></pre>
<h2 id="三-多态">三 多态</h2>
<p>多态是面向对象的三大特性之一，即一个类型具备多种具体的表现形式。</p>
<p>上述示例中，鸟和鱼都实现了动物接口的 Breath方法，即动物的Breath方法在鸟和鱼中具备不同的体现。我们在new出动物的具体对象实例时，这个对象实例也就实现了对应自己的接口方法。</p>
<pre><code class="language-go">// New出Animal的函数
func NewAnimal(kind string) Animal{

	switch kind {
	case &quot;鸟类&quot;:
		return &amp;Bird{}
	case &quot;鱼类&quot;:
		return &amp;Fish{}
	default:
		return nil
	}

}

func main() {
	// 获取的是动物接口类型，但是实现类是鸟类
	a1 := NewAnimal(&quot;鸟类&quot;)
	a1.Breath()		// 鸟 在 陆地 呼吸

	// 获取的是动物接口类型，但是实现类是鱼类
	a2 := NewAnimal(&quot;鱼类&quot;)
	a2.Breath()		// 鱼 在 水下 呼吸
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[19、接口类型-接口的使用]]></title>
        <id>https://keekuun.github.io/post/19jie-kou-lei-xing-jie-kou-de-shi-yong/</id>
        <link href="https://keekuun.github.io/post/19jie-kou-lei-xing-jie-kou-de-shi-yong/">
        </link>
        <updated>2023-01-04T09:06:59.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-接口-interface">一 接口 interface</h2>
<p>接口（interface）是调用方和实现方均需要遵守的一种约束，约束开发者按照统一的方法命名、参数类型、数量来处理具体业务。实际上，接口就是一组没有实现的方法声明，到某个自定义类型要使用该方法时，根据具体情况把这些方法实现出来。接口语法：</p>
<pre><code class="language-go">type 接口类型名 interface {
	方法名1(参数列表) 返回值列表
	方法名2(参数列表) 返回值列表
	...
}
</code></pre>
<p>示例：</p>
<pre><code class="language-go">package main

import &quot;fmt&quot;

// 运输方式
type Transporter interface {
	BicycleTran()
	CarTran()
}

// 驾驶员
type Driver struct {
	Name string
	Age  int
}

// 实现运输方式接口
func (d *Driver) BicycleTran() {
	fmt.Println(&quot;使用自行车运输&quot;)
}
func (d *Driver) CarTran() {
	fmt.Println(&quot;使用小汽车运输&quot;)
}

func main() {
	d := &amp;Driver{
		&quot;张三&quot;,
		27,
	}
	trans(d)
}

// 只要实现了 Transporter接口的类型都可以作为参数
func trans(t Transporter) {
	t.BicycleTran()
}

</code></pre>
<p>注意：</p>
<ul>
<li>Go语言的接口在命名时，一般会在单词后面添加er，如写操作的接口叫做Writer</li>
<li>当方法名首字母大写，且实现的接口首字母也是大写，则该方法可以被接口所在包之外的代码访问</li>
<li>方法与接口中的方法签名一致（方法名、参数列表、返回列表都必须一致）</li>
<li>参数列表和返回值列表中的变量名可以被忽略，如：type writer interfae{ Write([]byte) error}</li>
<li>接口中所有的方法都必须被实现</li>
<li>如果编译时发现实现接口的方法签名不一致，则会报错：<code>does not implement</code>。</li>
</ul>
<h2 id="二-go接口的特点">二 Go接口的特点</h2>
<p>在上述示例中，Go无须像Java那样显式声明实现了哪个接口，即为非侵入式，接口编写者无需知道接口被哪些类型实现，接口实现者只需要知道实现的是什么样子的接口，但无需指明实现了哪个接口。编译器知道最终编译时使用哪个类型实现哪个接口，或者接口应该由谁来实现。</p>
<p>类型和接口之间有一对多和多对一的关系，即：</p>
<ul>
<li>一个类型可以实现多个接口，接口间是彼此独立的，互相不知道对方的实现</li>
<li>多个类型也可以实现相同的接口。</li>
</ul>
<pre><code class="language-go">type Service interface {
	Start()
	Log(string)
}

// 日志器
type Logger struct {
}
//日志输出方法
func (g *Logger) Log(s string){
	fmt.Println(&quot;日志：&quot;, s)
}

// 游戏服务
type GameService struct {
	Logger
}
// 实现游戏服务的Start方法
func (g *GameService) Start() {
	fmt.Println(&quot;游戏服务启动&quot;)
}

func main() {
	s := new(GameService)
	s.Start()
	s.Log(&quot;hello&quot;)
}
</code></pre>
<p>在上述案例中，即使没有接口也能运行，但是当存在接口时，会隐式实现接口，让接口给类提供约束。</p>
<p>使用接口调用了结构体中的方法，也可以理解为实现了面向对象中的多态。</p>
<h2 id="三-接口嵌套">三 接口嵌套</h2>
<p>Go中不仅结构体之间可以嵌套，接口之间也可以嵌套。接口与接口嵌套形成了新的接口，只要接口的所有方法被实现，则这个接口中所有嵌套接口的方法均可以被调用。</p>
<pre><code class="language-go">// 定义一个 写 接口
type Writer interface {
	Write(p []byte) (n int, e error)
}

// 定义一个 读 接口
type Reader interface {
	Read() error
}

// 定义一个 嵌套接口
type IO interface {
	Writer
	Closer
}
</code></pre>
<h2 id="四-空接口">四 空接口</h2>
<h4 id="41-空接口定义">4.1 空接口定义</h4>
<p>空接口是接口的特殊形式，没有任何方法，因此任何具体的类型都可以认为实现了空接口。</p>
<pre><code class="language-go">	var any interface{}

	any = 1
	fmt.Println(any)

	any = &quot;hello&quot;
	fmt.Println(any)
</code></pre>
<p>空接口作为函数参数：</p>
<pre><code class="language-go">func Test(i interface{}) {
	fmt.Printf(&quot;%T\n&quot;, i)
}

func main() {
	Test(3)			// int
	Test(&quot;hello&quot;)	// sting
}
</code></pre>
<p>利用空接口，可以实现任意类型的存储：</p>
<pre><code class="language-go">	m := make(map[string]interface{})
	m[&quot;name&quot;] = &quot;李四&quot;
	m[&quot;age&quot;] = 30	
</code></pre>
<h4 id="42-从空接口获取值">4.2 从空接口获取值</h4>
<p>保存到空接口的值，如果直接取出指定类型的值时，会发生编译错误：</p>
<pre><code class="language-go">	var a int = 1
	var i interface{} = a
	var b int = i				//这里编译报错（类型不一致），可以这样做：b := i
</code></pre>
<h4 id="43-空接口值比较">4.3 空接口值比较</h4>
<p>类型不同的空接口比较：</p>
<pre><code class="language-go">	var a interface{} = 100
	var b interface{} = &quot;hi&quot;

	fmt.Println(a == b)			//false
</code></pre>
<p>不能比较空接口中的动态值：</p>
<pre><code class="language-go">	var c interface{} = []int{10}
	var d interface{} = []int{20}
	fmt.Println(c == d)					//运行报错
</code></pre>
<p>空接口的类型和可比较性：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>map</td>
<td>不可比较，会发生宕机错误</td>
</tr>
<tr>
<td>切片</td>
<td>不可比较，会发生宕机错误</td>
</tr>
<tr>
<td>通道</td>
<td>可比较，必须由同一个make生成，即同一个通道才是true</td>
</tr>
<tr>
<td>数组</td>
<td>可比较，编译期即可知道是否一致</td>
</tr>
<tr>
<td>结构体</td>
<td>可比较，可逐个比较结构体的值</td>
</tr>
<tr>
<td>函数</td>
<td>可比较</td>
</tr>
</tbody>
</table>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[18、面向对象-三大特性]]></title>
        <id>https://keekuun.github.io/post/18mian-xiang-dui-xiang-san-da-te-xing/</id>
        <link href="https://keekuun.github.io/post/18mian-xiang-dui-xiang-san-da-te-xing/">
        </link>
        <updated>2023-01-04T09:06:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-面向对象三大特性">一 面向对象三大特性</h2>
<h4 id="11-封装">1.1 封装</h4>
<p>封装：把抽象出的字段和对字段的操作封装在一起,数据被保护在内部,程序的其它包只有通过被授权的操作(方法),才能对字段进行修改，其作用有：</p>
<ul>
<li>隐藏实现细节</li>
<li>可以对数据进行验证，保证安全合理</li>
</ul>
<p>Golang对面向对象做了极大简化，并不强调封装特性，下列示例进行模拟实现：</p>
<p>在<code>person</code>包下新建<code>person.go</code>文件：</p>
<pre><code class="language-go">package person

import &quot;fmt&quot;

type person struct {
	Name string
	age int			//年龄是隐私，不允许其他包访问
}

//工厂函数（类似构造函数）
func NewPerson(name string) *person {
	return &amp;person{
		Name: name,
	}
}

func (p *person) SetAge(age int) {
	if age &gt; 0 &amp;&amp; age &lt; 150 {		//校验
		p.age = age
	} else {
		fmt.Println(&quot;年龄不合法&quot;)
	}
}

func (p *person) GetAge() int {
	return p.age
}
</code></pre>
<p><code>main.go</code>文件操作person：</p>
<pre><code class="language-go">package main

import (
	&quot;demo/person&quot;					// demo是go mod模式下，整体项目名
	&quot;fmt&quot;
)

func main() {
	p := person.NewPerson(&quot;Tom&quot;)
	p.SetAge(18)
	fmt.Println(p)
}
</code></pre>
<h4 id="12-继承">1.2 继承</h4>
<p>在 Golang 中，如果一个 struct 嵌套了另一个匿名结构体，那么这个结构体可以直接访 问匿名结构体的字段和方法，从而实现了继承特性。</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
)

type Father struct {
	Name string
	age int
}
func (f *Father) run() {
	fmt.Println(f.Name + &quot; like running...&quot;)
}

type Son struct {
	Father              //嵌套匿名结构体
}

func main() {

	var s Son

	//s.Father.Name = &quot;Tom&quot;
	//s.Father.age = 10     		//可以访问未导出属性
	//s.Father.run()          	//可以访问未导出方法

	//上述可以简写为：
	s.Name = &quot;Tom&quot;
	s.age = 10
	s.run()

}
</code></pre>
<p>注意：</p>
<ul>
<li>当结构体和匿名结构体有相同的字段或者方法时，编译器采用就近访问原则访问，如果希望访问匿名结构体的字段和方法，可以通过匿名结构体名来区分。</li>
<li>结构体嵌入多个匿名结构体，如果两个匿名结构体有相同的字段和方法(同时结构体本身没有同名的字段和方法)，访问时必须明确指定匿名结构体名字，否则编译报错。</li>
<li>如果一个 struct 嵌套了一个有名结构体，这种模式就是组合，如果是组合关系，那么在访问组合的结构体的字段或方法时，必须带上结构体的名字。</li>
</ul>
<p>关于多重继承：如果一个 struct 嵌套了多个匿名结构体，那么该结构体可以直接访问嵌套的匿名结构体的字段和方法，从而实现多重继承。</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
)

type Father1 struct {
	Name string
	age int
}
func (f *Father1) run() {
	fmt.Println(f.Name + &quot; like running...&quot;)
}

type Father2 struct {
	Like string
}


type Son1 struct {
	Father1
	Father2
}

type Son2 struct {
	*Father1
	*Father2
}

func main() {

	s1 := &amp;Son1 {
		Father1{
			Name: &quot;Tom&quot;,
			age: 10,
		},
		Father2{
			Like: &quot;伏特加&quot;,
		},
	}

	fmt.Println(s1)

	s2 := &amp;Son2{
		&amp;Father1{
			Name: &quot;Tom&quot;,
			age: 10,
		},
		&amp;Father2{
		 	Like: &quot;伏特加&quot;,
		},
	}
	fmt.Println(s2.Father1)

}
</code></pre>
<p>输出结果：</p>
<pre><code>&amp;{{Tom 10} {伏特加}}
&amp;{Tom 10}
</code></pre>
<h4 id="13-多态">1.3 多态</h4>
<p>多态与接口（interface）有关联，参见接口章节</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[17、面向对象-构造函数与方法]]></title>
        <id>https://keekuun.github.io/post/17mian-xiang-dui-xiang-gou-zao-han-shu-yu-fang-fa/</id>
        <link href="https://keekuun.github.io/post/17mian-xiang-dui-xiang-gou-zao-han-shu-yu-fang-fa/">
        </link>
        <updated>2023-01-04T09:06:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一-面向对象初识">一 面向对象初识</h2>
<h4 id="11-模拟构造函数">1.1 模拟构造函数</h4>
<p>Go和传统的面向对象语言如Java有着很大区别。结构体没有构造函数初始化功能，可以通过以下方式模拟：</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
)

type Person struct {
	Name string
	Age int
}

func NewPersonByName(name string) *Person {
	return &amp;Person{
		Name: name,
	}
}

func NewPersonByAge(age int) *Person {
	return &amp;Person{
		Age: age,
	}
}

func main() {

	p := NewPersonByName(&quot;zs&quot;)
	fmt.Println(p)						// {zs 0}

}
</code></pre>
<p>贴士：因为Go没有函数重载，为了避免函数名字冲突，使用了<code>NewPersonByName</code>和<code>NewPersonByAge</code>两个不同的函数表示不同的<code>Person</code>构造过程。</p>
<h4 id="12-父子关系结构体初始化">1.2 父子关系结构体初始化</h4>
<p>Person可以看做父类，Student是子类，子类需要继承父类的成员：</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
)

type Person struct {
	Name string
	Age int
}

type Student struct {
	Person
	ClassName string
}

//构造父类
func NewPerson(name string, age int) *Person {
	return &amp;Person{
		Name: name,
		Age: age,
	}
}

//构造子类
func NewStudent(classname string) *Student {
	p := &amp;Student{}
	p.ClassName = classname
	return p
}


func main() {

	s := NewStudent(&quot;一班&quot;)
	fmt.Println(s)						// &amp;{{ 0} 一班}

}
</code></pre>
<h4 id="13-go中的面向对象初识">1.3 Go中的面向对象初识</h4>
<p>在Go中，可以给任意类型（除了指针）添加相应方法：</p>
<pre><code class="language-go">type Interger int

func (i Interger) Less (j Interger) bool {
	return i &lt; j
}

func main() {
	var i Interger = 1
	fmt.Print(i.Less(5))
}
</code></pre>
<h2 id="二-方法">二 方法</h2>
<h4 id="21-方法">2.1 方法</h4>
<p>Golang 中的方法是作用在指定的数据类型上的(即:和指定的数据类型绑定)，因此自定义类型，都可以有方法，而不仅仅是 struct。</p>
<p>方法的声明和调用：</p>
<pre><code class="language-go">func (recevier type) methodName(参数列表) (返回值列表){ 
    //方法体
    return 返回值
}
</code></pre>
<p>方法与函数的示例：</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
)

type Person struct {
	Name string
	Age int
}

// 一个run函数
func run(p *Person, name string) {
	p.Name = name
	fmt.Println(&quot;函数 run...&quot;, p.Name)
}

// 一个run方法
func (p *Person)run() {
	fmt.Println(&quot;方法 run...&quot;, p.Name)
}

func main() {

	// 实例化一个对象（结构体）
	p1 := &amp;Person{
		&quot;ruyue&quot;,
		10,
	}

	// 执行一个普通方法
	run(p1, &quot;张三&quot;)			// 输出 函数 run... 张三

	// 执行方法
	p1.run()						// 输出 方法 run... 张三

}
</code></pre>
<h4 id="22-go方法本质">2.2 Go方法本质</h4>
<p>Go的方法是一种作用于特定类型变量的函数，这种特定类型的变量叫做接收器（Receiver）。如果特定类型理解为结构体或者“类”时，接收器就类似于其他语言的this或者self。</p>
<p>在Go中，接收器可以是任何类型，不仅仅是结构体，依此我们看出，Go中的方法和其他语言的方法类似，但是Go语言的接收器强调方法的作用对象是实例。</p>
<p>方法与函数的区别就是：函数没有作用对象。</p>
<p>指针接收器传入的是 struct 本身，指针接收器可以读写 struct 中的内容，在方法结束后，修改都是有效的。</p>
<p>非指针接收器传入的是 struct 的 copy 副本，非指针接收器只能读取 struct 中的数据但是不能写入，如果写入的话也只是写入到 struct 的备份中而已。</p>
<p>示例如下:</p>
<pre><code class="language-go">package main

import &quot;fmt&quot;

type student struct {
	age int8
}

//指针接收器
func(s *student) ageAdd1() {
	s.age += 1
}

//非指针接收器
func(s student) ageAdd2() {
	s.age += 1
}

func main() {
	student := new(student)

	student.ageAdd1()
	fmt.Println(student.age) // 1 传入指针，原值 + 1，为 1

	student.ageAdd1()
	fmt.Println(student.age) // 2 传入指针，原值 + 1，为 2

	student.ageAdd2()
	fmt.Println(student.age) // 2 传入复制体，复制体 + 1，所以原值还是 2
}
</code></pre>
<p>一般情况下，小对象由于复制时速度较快，适合使用非指针接收器，大对象因为复制性能较低，适合使用指针接收器，此时再接收器和参数之间传递时不进行复制，只传递指针。</p>
]]></content>
    </entry>
</feed>